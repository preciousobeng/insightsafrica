# InsightsAfrica Session — 2026-06-14

## What was done

### #10 Flood Templates (committed 37351b8)

- Shared flood.html template: Nigeria + Senegal byte-identical
- 4 variant templates: Ghana (infra layer), Ivory Coast (inverted logic), South Africa (crossfade displayLayer), Cape Verde (single-level)
- Generator variant mechanism confirmed working
- All 6 countries byte-verified
- Normalized all flood pages to LF line endings

### #10 All page types generator-driven (committed c81466e)

- Shared templates + per-country variants for all 7 types: flood, crop, heat, mine, human, profile, hub
- 42 pages (7 types x 6 countries) now generated from scripts/build_pages.py

### Flicker-free displayLayer made canonical in shared flood template

- Adopted South Africa's flicker-free overlay swap (fade the next tile in at
  opacity 0, only drop the previous overlay on load) plus tile preload into the
  shared flood.html.
- Regenerates Nigeria + Senegal with this as their only change. SA flood already
  had it; it was the source of the pattern.

## Outstanding — pick up next session

1. Propagate flicker-free displayLayer beyond the shared flood template.
   The swap currently lives only in flood.html (NG/SN) and SA's flood variant.
   The flood variants for Ghana / Ivory Coast / Cape Verde, and ALL of the other
   six page types (crop, heat, mine, human, profile, hub) that render map overlays,
   still use the old remove-then-add swap and will flicker. Audit each for a
   displayLayer / overlay function and apply the same pattern.

2. Reduce flood variant count by parameterizing the shared template.
   SA and IC are full ~1.3k-line snapshots, not parameterized. SA can fold into
   the shared template via a coarse_empty_name flag (empty p.name on provinces,
   name in p.region) — that work exists in dropped local commit 74861fc, recover
   it from the git reflog. Goal from the original note: parameterize shared
   templates, then delete variants that generate correctly.

3. Resolve the Ivory Coast level-inversion contradiction.
   countries.yml says IC is districts=coarse(14) / regions=fine(31) — geographically
   correct. But the committed (now variant-snapshotted) IC page treats districts
   as the FINE level and labels the coarse level "Region". Before IC can fold into
   the shared template, confirm against the actual anomaly data / boundary key
   structure which interpretation the data uses, then make page + config agree.
   Also a latent template bug: a single-quoted JS string interpolating
   display_name breaks on the apostrophe in "Cote d'Ivoire" — use double quotes
   or the tojson filter.

## How to regenerate / verify

    PYTHONIOENCODING=utf-8:replace ./venv/bin/python scripts/build_pages.py --type <type> --verify
    PYTHONIOENCODING=utf-8:replace ./venv/bin/python scripts/build_pages.py --all
